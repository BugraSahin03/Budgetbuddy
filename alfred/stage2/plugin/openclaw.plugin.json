{
  "id": "budgetbuddy-snapshot",
  "contracts": {
    "tools": [
      "budgetbuddy_snapshot"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "snapshotPath": {
        "type": "string",
        "minLength": 1
      },
      "maxAgeMinutes": {
        "type": "integer",
        "minimum": 1,
        "maximum": 1440
      }
    }
  },
  "uiHints": {
    "snapshotPath": {
      "label": "BudgetBuddy snapshot",
      "help": "Fixed path to the collector-owned read-only JSON snapshot."
    },
    "maxAgeMinutes": {
      "label": "Maximum snapshot age",
      "help": "Reject older data instead of silently treating it as current."
    }
  }
}
